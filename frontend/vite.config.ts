import react from "@vitejs/plugin-react";
import { defineConfig } from "vitest/config";

export default defineConfig({
  plugins: [react()],
  // В разработке фронт и бэк живут на разных портах. Прокси избавляет от CORS
  // и заодно делает куку сессии однодоменной — иначе браузер её не сохранит.
  // Адрес бэкенда переопределяется переменной окружения: порт 8000 бывает
  // занят уже поднятым контейнером, и тогда единственная альтернатива —
  // править конфигурацию руками и не забыть вернуть.
  server: {
    proxy: { "/api": process.env.API_TARGET ?? "http://localhost:8000" },
  },
  test: {
    environment: "jsdom",
    setupFiles: ["./src/test/setup.ts"],
    globals: true,
    // Втрое больше того, сколько ждёт Testing Library (см. src/test/setup.ts).
    // Пять секунд по умолчанию совпадали с её ожиданием ровно, и под полной
    // сборкой, где два десятка файлов делят ядра, тест умирал по таймауту
    // раньше, чем ожидание успевало сказать, чего именно оно не дождалось:
    // вместо внятного «не нашёл такой элемент» — «test timed out» на строке
    // с названием теста. На зелёном прогоне запас не стоит ничего.
    testTimeout: 15_000,
  },
});
