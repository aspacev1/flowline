# Flowline

Самостоятельно размещаемый планировщик проектов с диаграммой Ганта: категории,
задачи, сроки, критичность и история изменений с отменой. Организация — единица
изоляции данных; регистрация свободная, первый вошедший получает собственную
организацию и роль `owner`.

## Запуск

Нужен Docker с плагином `compose`.

```sh
cp .env.example .env      # отредактируй APP_SECRET и учётные данные Postgres
docker compose up
```

Интерфейс открывается на <http://localhost:8080> — там регистрация, вход и
список проектов. Порт меняется переменной `WEB_PORT`.

Фронтенд и API живут за одним доменом: Caddy отдаёт статику с корня, а всё,
что начинается с `/api/`, проксирует в бэкенд. Это не украшательство —
сессия хранится в HTTP-only куке, и на двух разных доменах браузер её просто
не сохранил бы. По той же причине адреса вроде `/projects` отдают
`index.html`: маршруты живут на клиенте, файлов под них нет, и без этого
перезагрузка страницы внутри приложения давала бы 404.

API доступен и напрямую на <http://localhost:8000>, проверка живости — `GET
/api/health`. Схема базы накатывается автоматически (`alembic upgrade head`
выполняется перед стартом сервера), отдельного шага миграции нет.

Программно: `POST /api/auth/register` с полями `name`, `email`, `password`
ставит куку сессии, и с ней доступны `POST /api/projects`,
`GET /api/projects/{id}` и `POST /api/projects/{id}/mutations`.

## Публичные ссылки

Проект открывается наружу с экрана его настроек. Адрес собран из слага
организации и слага проекта — `/p/acme/redesign-2026`, — и потому известен
заранее, ещё до публикации. Секрета в адресе нет: отзыв закрывает страницу
мгновенно, но повторная публикация оживляет тот же самый адрес, а проект с
включённой ссылкой открывает всякий, кто оба слага угадает. Настоящий
перевыпуск — переименование слага проекта там же, в настройках: прежний адрес
после него не открывается.

Гость видит ту же диаграмму без инструментов правки; внутренние заметки и
состав организации наружу не выходят вовсе — их нет в ответе сервера. Если
владелец не выключил комментарии, гость представляется именем (браузер его
запоминает) и пишет в обсуждение проекта. Частота гостевых реплик ограничена
по адресу: потолок задаёт `GUEST_COMMENT_RATE_LIMIT`, окно — час. Счётчик
живёт в памяти процесса, поэтому при нескольких процессах `api` потолок
умножается на их число.

Адрес, который показывает экран настроек, собирается из `PUBLIC_BASE_URL`:
это значение должно указывать на тот адрес, по которому открывается интерфейс
(в разработке — `http://localhost:8080`, а не порт бэкенда).

## Разработка интерфейса

```sh
cd frontend && npm install && npm run dev
```

Dev-сервер поднимается на <http://localhost:5173> и проксирует `/api` на
`http://localhost:8000` — тот же однодоменный расклад, что и в бою. Если
бэкенд слушает на другом порту, адрес задаётся переменной `API_TARGET`.
Тесты интерфейса — `npm test` в `frontend/`; они бьют по перехваченной сети,
базы и бэкенда им не нужно.

`.env` — единственный источник настроек, и в `.env.example` каждая описана
комментарием. Два значения обязательны и не имеют умолчания: `DATABASE_URL` и
`APP_SECRET`.

## Тесты

Тесты работают только с базой `<имя из DATABASE_URL>_test` и никогда с базой
из `DATABASE_URL` — `backend/tests/conftest.py` это проверяет и отказывается
запускаться иначе. Тестовую базу нужно создать один раз:

```sh
docker compose exec db createdb -U flowline flowline_test
cd backend && uv run pytest
```
