# Зависимости serverless-функции на Vercel: сборщик Python читает именно
# requirements.txt и про pyproject.toml с uv.lock ничего не знает. Версии
# закреплены по backend/uv.lock — чтобы бой и локальная разработка собирались
# из одного набора, а не из «что оказалось свежим в день деплоя».
#
# При изменении зависимостей в backend/pyproject.toml обнови и этот файл.
#
# Здесь только то, что нужно на запрос. uvicorn не нужен — ASGI-приложение
# запускает сама платформа; alembic не нужен — миграции накатываются отдельной
# командой с машины разработчика (см. раздел про Vercel в README).
fastapi==0.141.1
sqlalchemy==2.0.51
psycopg[binary]==3.3.4
pydantic==2.13.4
pydantic-settings==2.15.0
argon2-cffi==25.1.0
email-validator==2.3.0
cryptography==50.0.0
pyyaml==6.0.3
